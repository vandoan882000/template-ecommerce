{% import "../../shared/card/card.twig" as card %}
{% import "../../shared/category/category.twig" as category %}
<section class="page-section--small">
  <div class="container">
  <h3 class="page-section__header">New Arrivals</h3>
    <div class="row">
      {% for item in productSection.product %}
        <div class="col-xs-12 col-sm-5 col-md-4 col-lg-3">
          {{card.render(item)}}
        </div>
      {% endfor %}

    </div>
    <button class="btn btn__primary" style="margin-top: 30px;margin-bottom: 70px;">LOAD MORE</button>
  </div>
</section>
