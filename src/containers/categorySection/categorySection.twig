{% import "../../shared/category/category.twig" as category %}

<section class="page-section--small">
  <div class="container">
    <h2 class="page-section__header">Top Categories</h2>
    <div class="row">
      {% for item in categorySection.category %}
        <div class="col-xs-10 col-sm-5 col-md-4 col-lg-3">
        {{ category.render(item)}}
      </div>
      {% endfor %}

    </div>
  </div>
</section>
