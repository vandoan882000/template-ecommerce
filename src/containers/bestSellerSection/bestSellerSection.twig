{% import "../../shared/card/card.twig" as card %}
<section class="page-section--small">
  <div class="container">
  <h3 class="page-section__header">Best Seller</h3>
    <div class="row">
      {% for item in bestSellerSection.product %}
        <div class="col-xs-12 col-sm-5 col-md-4 col-lg-3">
          {{card.render(item)}}
        </div>
      {% endfor %}
    </div>
  </div>
</section>
