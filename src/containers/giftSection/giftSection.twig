{% import "../../shared/gift/gift.twig" as gift %}

<nav class="page-section" style="padding: 0;">
	<div class="container" style="background-color: #FFE7D4;">
		<div class="row">
			{% for item in giftSection %}
				<div class="col-xs-12 col-sm-6 col-md-16 col-lg-3">
					{{gift.render(item)}}
				</div>
			{% endfor %}
		</div>
	</div>
</nav>
