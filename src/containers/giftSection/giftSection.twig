{% import "../../shared/gift/gift.twig" as gift %}

<nav class="page-section" style="background-color: #FFE7D4;padding: 0px 60px 0px 60px;">
	<div class="container" >
		<div class="row">
			{% for item in giftSection %}
				<div class="col-xs-12 col-sm-6 col-md-16 col-lg-3">
					{{gift.render(item)}}
				</div>
			{% endfor %}
		</div>
	</div>
</nav>
