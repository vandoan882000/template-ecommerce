{% macro render(data) %}
  <!--Trend-->
  <div class="trend">
    <a href="#"><img class="trend__img" src={{data.srcImg}} alt="trend"></a>
    <div class="trend__content--{{data.type}}">
      <div>{{data.name}}</div>
    </div>
  </div>
  <!--End / Trend-->
{% endmacro %}
